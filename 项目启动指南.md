# Smart Esports Hotel Management System - 项目启动指南

## 📋 目录
- [系统概述](#系统概述)
- [依赖环境验证](#依赖环境验证)
- [项目启动流程](#项目启动流程)
- [常见问题解决](#常见问题解决)
- [API 文档访问](#api-文档访问)

---

## 🎯 系统概述

**智慧电竞酒店管理系统** 是一个前后端分离的全栈项目：
- **后端**：Spring Boot 3.1.5 + MyBatis-Plus + MySQL + Redis + WebSocket
- **前端**：Vue 3 + Vite + Element Plus + Axios
- **功能**：用户认证、房间管理、硬件实时监控、WebSocket 实时推送

---

## ✅ 依赖环境验证

### 1. Java 环境

**检查命令：**
```powershell
java -version
```

**要求版本：**
- Java 21.0.8 或更高版本

**验证结果示例：**
```
java version "21.0.8" 2024-10-15 LTS
Java(TM) SE Runtime Environment (build 21.0.8+9-LTS-261)
```

---

### 2. Maven 环境

**检查命令：**
```powershell
mvn -version
```

**要求版本：**
- Apache Maven 3.9.11 或更高版本

**验证结果示例：**
```
Apache Maven 3.9.11
Maven home: D:\apache-maven-3.9.11
Java version: 21.0.8
```

---

### 3. Node.js 和 npm

**检查命令：**
```powershell
node -v
npm -v
```

**要求版本：**
- Node.js: 22.17.1 或更高版本
- npm: 11.4.2 或更高版本

**验证结果示例：**
```
v22.17.1
11.4.2
```

---

### 4. MySQL 数据库

**检查服务状态：**
```powershell
Get-Service MySQL80
```

**启动 MySQL 服务：**
```powershell
mysql -u root -pmmmm8888 -e "SELECT 'MySQL连接成功' AS Status; USE esports_hotel_db; SELECT COUNT(*) AS TableCount FROM information_schema.tables WHERE table_schema='esports_hotel_db';"
```
```powershell
Start-Service MySQL80
```

**验证连接：**
```powershell
mysql -u root -p
# 输入密码：mmmm8888
```

**要求配置：**
- MySQL 8.0
- 端口：3306
- 用户名：root
- 密码：mmmm8888（已在 application.yml 中配置）
- 数据库：esports_hotel_db（包含 26 张表）

---

### 5. Redis 服务

**启动 Redis：**
```powershell
cd "D:\Program Files (x86)\Redis\Redis-8.0.5-Windows-x64-msys2-with-Service"; Start-Process .\redis-server.exe -NoNewWindow -PassThru | Out-Null; Start-Sleep -Seconds 3; redis-cli ping
```

```powershell
cd D:\Redis-8.0.5-Windows-x64
.\redis-server.exe
```

**验证 Redis 连接：**
```powershell
redis-cli ping
# 应返回：PONG
```

**要求配置：**
- Redis 8.0.5
- 端口：6379
- 无密码

---

## 🚀 项目启动流程

### 方法一：标准启动（推荐用于开发调试）

#### 第 1 步：启动后端服务

**打开第一个 PowerShell 终端：**

```powershell
Start-Process powershell -ArgumentList "-NoExit", "-Command", "[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; `$env:JAVA_TOOL_OPTIONS='-Dfile.encoding=UTF-8'; cd d:\java_project2\backend; mvn spring-boot:run"
```

```powershell
# 设置控制台编码为 UTF-8（解决中文乱码）
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8

# 设置 Java 文件编码
$env:JAVA_TOOL_OPTIONS='-Dfile.encoding=UTF-8'

# 进入后端目录
cd d:\java_project2\backend

# 启动后端应用
java -jar target/esports-hotel-backend.jar
```

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; 

$env:JAVA_TOOL_OPTIONS='-Dfile.encoding=UTF-8'; 

cd d:\java_project2\backend; mvn spring-boot:run
```

**等待启动完成，看到以下信息表示成功：**

```
╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║   Smart Esports Hotel Management System Started!         ║
║   智慧电竞酒店管理系统启动成功                               ║
║                                                           ║
║   Swagger UI: http://localhost:8080/api/swagger-ui.html  ║
║   API Docs:   http://localhost:8080/api/v3/api-docs      ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
```

**后端服务信息：**
- 端口：8080
- 上下文路径：/api
- API 前缀：http://localhost:8080/api
- Swagger 文档：http://localhost:8080/api/swagger-ui.html

---

#### 第 2 步：启动前端服务

**打开第二个 PowerShell 终端：**

```powershell
# 进入前端目录
cd d:\java_project2\frontend

# 启动开发服务器
npm run dev
```

**等待启动完成，看到以下信息：**

```
  VITE v5.4.21  ready in 1931 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

**前端服务信息：**
- 端口：5173（如果被占用会自动使用 5174）
- 访问地址：http://localhost:5173/
- 支持热更新（修改代码自动刷新）

---

#### 第 3 步：访问系统

**在浏览器中打开：**
```
http://localhost:5173/
```

---

### 方法二：后台启动（推荐用于演示）

**一键启动脚本：**

```powershell
# 启动后端（后台运行）
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:JAVA_TOOL_OPTIONS='-Dfile.encoding=UTF-8'
cd d:\java_project2\backend
Start-Process -NoNewWindow java -ArgumentList '-jar', 'target/esports-hotel-backend.jar'

# 等待 5 秒让后端启动
Start-Sleep -Seconds 5

# 启动前端
cd d:\java_project2\frontend
npm run dev
```

---

## 🔧 常见问题解决

### 问题 1：后端中文日志乱码

**现象：**
```
2025-12-16 10:40:35 [main] INFO - 纭欢鏁版嵁鐢熸垚...
```

**解决方案：**
在启动 Java 应用前设置编码：

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:JAVA_TOOL_OPTIONS='-Dfile.encoding=UTF-8'
```

---

### 问题 2：前端验证码请求失败

**现象：**
点击"获取验证码"显示"请求资源不存在"

**解决方案：**
已修复。前端 API 路径已从 `/auth/send-sms-code` 更改为 `/auth/sms/send`，与后端保持一致。

---

### 问题 3：MySQL 连接失败

**现象：**
```
Access denied for user 'root'@'localhost'
```

**解决方案：**
1. 确认 MySQL 服务已启动：
   ```powershell
   Start-Service MySQL80
   ```

2. 确认密码正确（已配置为 `mmmm8888`）

3. 检查配置文件 `backend/src/main/resources/application.yml`：
   ```yaml
   spring:
     datasource:
       username: root
       password: mmmm8888
   ```

---

### 问题 4：Redis 连接失败

**现象：**
```
Unable to connect to Redis
```

**解决方案：**
启动 Redis 服务：
```powershell
cd D:\Redis-8.0.5-Windows-x64
.\redis-server.exe
```

验证连接：
```powershell
redis-cli ping
# 应返回：PONG
```

---

### 问题 5：端口被占用

**后端端口 8080 被占用：**
```powershell
# 查找占用端口的进程
netstat -ano | findstr :8080

# 关闭进程（替换 PID）
taskkill /PID <进程ID> /F
```

**前端端口 5173 被占用：**
Vite 会自动尝试下一个端口（5174、5175...）

---

### 问题 6：数据库未初始化

**现象：**
```
Table 'esports_hotel_db.tb_user' doesn't exist
```

**解决方案：**
重新导入数据库：
```powershell
# 方式一：命令行导入
mysql -u root -pmmmm8888 esports_hotel_db < d:\java_project2\database\schema.sql

# 方式二：使用 UTF-8 编码导入
Get-Content d:\java_project2\database\schema.sql -Raw -Encoding UTF8 | mysql -u root -pmmmm8888 esports_hotel_db --default-character-set=utf8mb4
```

---

### 问题 7：MyBatis-Plus 版本不兼容

**现象：**
```
IllegalArgumentException: Invalid value type for attribute 'factoryBeanObjectType'
```

**解决方案：**
已修复。Spring Boot 版本已从 3.2.0 降级到 3.1.5，MyBatis-Plus 版本已升级到 3.5.9。

如需重新编译：
```powershell
cd d:\java_project2\backend
mvn clean install -DskipTests
```

---

## 📚 API 文档访问

### Swagger UI（推荐）

**访问地址：**
```
http://localhost:8080/api/swagger-ui.html
```

**功能：**
- 可视化 API 文档
- 在线测试接口
- 查看请求/响应示例

**主要 API 分组：**
1. **认证管理**（/auth）
   - POST /auth/sms/send - 发送短信验证码
   - POST /auth/register - 用户注册
   - POST /auth/login - 用户登录

2. **房间管理**（/rooms）
   - GET /rooms - 获取房间列表
   - POST /rooms/checkin - 办理入住
   - POST /rooms/checkout - 办理退房

3. **硬件监控**（/hardware）
   - GET /hardware/status/{roomId} - 获取硬件状态
   - GET /hardware/alerts - 获取告警列表

4. **WebSocket 实时推送**
   - 连接地址：ws://localhost:8080/api/ws
   - 订阅主题：/topic/hardware/{roomId}

---

## 🎮 系统功能演示

### 1. 用户注册

1. 打开 http://localhost:5173/
2. 点击"注册账号"
3. 输入手机号（如 `13800138000`）
4. 点击"获取验证码"
5. **在后端控制台查看验证码**（6 位数字）：
   ```
   2025-12-16 10:45:30 [http-nio-8080-exec-1] INFO - 模拟短信验证码: 手机号 13800138000, 验证码 123456
   ```
6. 输入验证码、密码、姓名等信息
7. 完成注册

---

### 2. 用户登录

1. 使用注册的手机号和密码登录
2. 登录成功后自动跳转到仪表盘
3. 可以看到系统概览信息

---

### 3. 房间管理

1. 点击左侧菜单"房间管理"
2. 查看所有房间状态（空闲/占用/维护）
3. 选择空闲房间办理入住
4. 输入住客信息完成入住
5. 系统会生成 Room-Auth-Token 用于硬件访问

---

### 4. 硬件实时监控

1. 点击左侧菜单"硬件监控"
2. 查看所有房间的硬件状态
3. **实时数据更新**（通过 WebSocket 每 5 秒推送）：
   - CPU 温度
   - GPU 温度
   - 网络延迟
   - 外设状态（键盘/鼠标/耳机）
4. 健康等级显示：
   - 🟢 GREEN：正常
   - 🟡 YELLOW：预警
   - 🔴 RED：故障（会自动创建告警工单）

---

## 🛑 关闭项目

### 关闭前端

**方法一：在终端按键**
```
按 q + Enter 或 Ctrl + C
```

**方法二：强制关闭**
```powershell
Get-Process node | Where-Object {$_.MainWindowTitle -like "*vite*"} | Stop-Process
```

---

### 关闭后端

**方法一：在终端按键**
```
Ctrl + C
```

**方法二：强制关闭所有 Java 进程**
```powershell
Get-Process java | Stop-Process -Force
```

**方法三：精确关闭后端进程**
```powershell
Get-Process java | Where-Object {$_.Path -like "*esports-hotel-backend*"} | Stop-Process
```

---

## 📊 项目结构

```
d:\java_project2\
├── backend/                          # 后端项目
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/
│   │   │   │   └── com/esports/hotel/
│   │   │   │       ├── controller/   # 控制器层
│   │   │   │       ├── service/      # 业务逻辑层
│   │   │   │       ├── mapper/       # 数据访问层
│   │   │   │       ├── entity/       # 实体类
│   │   │   │       ├── dto/          # 数据传输对象
│   │   │   │       ├── common/       # 通用类
│   │   │   │       └── config/       # 配置类
│   │   │   └── resources/
│   │   │       ├── application.yml   # 应用配置
│   │   │       └── banner.txt        # 启动横幅
│   │   └── test/                     # 测试代码
│   ├── target/
│   │   └── esports-hotel-backend.jar # 打包后的可执行 JAR
│   └── pom.xml                       # Maven 配置
│
├── frontend/                         # 前端项目
│   ├── src/
│   │   ├── api/                      # API 请求封装
│   │   │   ├── auth.js               # 认证 API
│   │   │   ├── room.js               # 房间 API
│   │   │   └── hardware.js           # 硬件 API
│   │   ├── views/                    # 页面组件
│   │   │   ├── Login.vue             # 登录页
│   │   │   ├── Dashboard.vue         # 仪表盘
│   │   │   ├── RoomManagement.vue    # 房间管理
│   │   │   └── HardwareMonitor.vue   # 硬件监控
│   │   ├── router/                   # 路由配置
│   │   ├── store/                    # 状态管理
│   │   ├── utils/                    # 工具函数
│   │   ├── main.js                   # 入口文件
│   │   └── App.vue                   # 根组件
│   ├── public/                       # 静态资源
│   ├── index.html                    # HTML 模板
│   ├── package.json                  # npm 配置
│   └── vite.config.js                # Vite 配置
│
├── database/
│   └── schema.sql                    # 数据库初始化脚本
│
├── SRS.txt                           # 软件需求规格说明书
└── 项目启动指南.md                    # 本文档
```

---

## 🔗 相关链接

- **前端访问地址**：http://localhost:5173/
- **后端 API 地址**：http://localhost:8080/api
- **Swagger 文档**：http://localhost:8080/api/swagger-ui.html
- **WebSocket 连接**：ws://localhost:8080/api/ws

---

## 📝 版本信息

- **项目版本**：1.0.0-SNAPSHOT
- **Spring Boot**：3.1.5
- **MyBatis-Plus**：3.5.9
- **Vue**：3.4.0
- **Element Plus**：2.5.0
- **更新日期**：2025-12-16

---

## 💡 开发提示

1. **热更新**：前端修改代码后自动刷新，后端需要重新编译
2. **日志级别**：开发环境默认 DEBUG，生产环境改为 INFO
3. **验证码有效期**：5 分钟
4. **Token 过期时间**：24 小时
5. **硬件数据推送频率**：每 5 秒一次
6. **数据库连接池**：HikariCP（最大连接数 10）

---

## 🎉 项目启动成功标志

✅ 后端控制台显示启动横幅  
✅ Swagger UI 可以访问  
✅ 前端页面正常显示  
✅ 可以成功注册和登录  
✅ 硬件监控数据实时更新  
✅ 中文日志正常显示（无乱码）

---

**如有问题，请检查上述依赖环境和常见问题解决方案。**
